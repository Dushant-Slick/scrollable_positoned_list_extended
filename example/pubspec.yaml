name: scrollable_positioned_list_extended_example
publish_to: 'none'

environment:
  sdk: '>=2.12.0-0 <3.0.0'
  flutter: '>=1.13.8'

dependencies:
  flutter:
    sdk: 'flutter'
  scroll_to_index: ^3.0.1
  scrollable_positioned_list_extended:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: 'flutter'
